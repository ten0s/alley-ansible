rename table Admins to admins;
rename table AuditEvents to auditevents;
rename table BackendItems to backenditems;
rename table LogEvents to logevents;
rename table Notifications to notifications;
rename table PasswordResetRequests to passwordresetrequests;
rename table __MigrationHistory to __migrationhistory;
