To setup whole cluster LIKE prod (except MongoDB) see README.cluster

To setup MongoDB replica LIKE prod see README.replset
Migration to MongoDB from official repo and upgrade procedure
described here.
